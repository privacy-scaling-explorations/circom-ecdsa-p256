import assert from 'assert';
// import { Point } from '../src/turtle';
// import { chordLength, distance, findPath } from '../src/turtlesoup';

describe.only('testA', function () {
  /** partitions
   * angle is acute, right, or obtuse
   * result is an integer vs. decimal
   */
  it('test1', function () {
    assert(true);
  });
  it('test2', function () {
    assert(true);
  });
  it('test3', function () {
    assert(true);
  });
});
